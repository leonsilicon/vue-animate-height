{
	"name": "vue-animate-height-monorepo",
	"type": "module",
	"scripts": {
		"prepare": "lion-git-hooks",
		"build": "pnpm run build --filter=./vue-animate-height",
		"test": "pnpm run test --filter=./vue-animate-height",
		"lint": "pnpm recursive run lint",
		"prettier": "pnpm recursive run prettier",
		"tc": "pnpm recursive run tc"
	},
	"devDependencies": {
		"@leonzalion/configs": "^1.2.3",
		"@types/node": "^17.0.23",
		"eslint-define-config": "^1.3.0",
		"typescript": "^4.6.3"
	}
}
